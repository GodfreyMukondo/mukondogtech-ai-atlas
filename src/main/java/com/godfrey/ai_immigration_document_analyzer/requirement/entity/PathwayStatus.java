package com.godfrey.ai_immigration_document_analyzer.requirement.entity;

/** Lifecycle of a Pathway catalogue entry itself - never a person's application/case status. */
public enum PathwayStatus {

    DRAFT,
    PUBLISHED,
    RETRACTED
}
